//*********************************
void keyPressed()
{
  loop();
  //start = true;
  // discover keys....
  // println((int)key, hex(key), keyCode);
  // save image
  if (key=='z'||key=='Z') { saveImage(); }
}
